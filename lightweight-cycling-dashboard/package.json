{
  "name": "lightweight-cycling-dashboard",
  "version": "1.0.0",
  "description": "A lightweight web application that cycles between Google Looker Studio dashboard and animated space-themed leaderboards",
  "main": "index.html",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:watch": "node --test --watch tests/*.test.js"
  },
  "devDependencies": {
    "fast-check": "^3.15.0",
    "jsdom": "^27.3.0"
  },
  "type": "module"
}
